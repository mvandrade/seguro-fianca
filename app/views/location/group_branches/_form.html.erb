<div style="min-height: 550px;">
    <div class="row">
        <div class="col-lg-12">
            <h4 class="page-header">Grupos de Ramos</h4>
        </div>
        <!-- /.col-lg-12 -->
    </div>

    <% if @group_branch.errors.any? %>
        <div class="alert alert-danger">
            <ul>
                <% @group_branch.errors.full_messages.each do |message| %>
                <li> <%= message %> </li>
                <% end %>
            <ul>
        </div>
    <% end %>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-8">
            <%= form_for([:location, @group_branch]) do |form| %>
                <div class="panel panel-default">
                    <div class="panel-heading col-lg-12" style="height: 50px" >
                    <% if @group_branch.id? %>
                        Grupo de Ramo (Editar)
                    <% else %>
                        Grupo de Ramo (Novo)
                    <% end %>
                    </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body ">

                        <div class="row">
                            <div class="col-lg-2" style="margin-top: 10px">
                                <div class="form-group">
                                    <label>Código</label>
                                    <%= form.text_field :code, class: "form-control" %>
                                </div>
                            </div>
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label>Descrição</label>
                                    <%= form.text_field :description, class: "form-control" %>
                                </div>
                            </div>
                        </div>
                        <!-- /.col-lg-12 -->
                        <%= form.submit 'Salvar', class: "btn btn-primary"  %>
                        <%= link_to 'Voltar', location_group_branches_path, class: "btn btn-default" %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>

